version: 2.1

jobs:
  test:
    docker:
      - image: cimg/node:23.10.0
    steps:
      - checkout
      - run: 
          name: Install dependencies
          command: npm install
      - run:
          name: Run tests
          command: npm run test

workflows:
  version: 2
  build_deploy:
    jobs:
      - test

